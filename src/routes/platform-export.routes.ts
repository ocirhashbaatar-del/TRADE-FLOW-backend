import{Router}from'express';import{prisma}from'../lib/prisma.js';const router=Router();router.get('/:token',async(req,res)=>{const job=await prisma.tenantExportJob.findFirst({where:{id:String(req.params.token),status:'READY',expiresAt:{gt:new Date()}}});if(!job)return res.status(404).json({message:'Export олдсонгүй эсвэл хугацаа дууссан.'});const id=job.tenantId,[tenant,users,products,categories,orders,invoices,payments,movements,audits]=await Promise.all([prisma.tenant.findUnique({where:{id}}),prisma.user.findMany({where:{tenantId:id},select:{id:true,name:true,email:true,phone:true,role:true,createdAt:true}}),prisma.product.findMany({where:{tenantId:id}}),prisma.category.findMany({where:{tenantId:id}}),prisma.order.findMany({where:{tenantId:id},include:{items:true,statusHistory:true}}),prisma.invoice.findMany({where:{tenantId:id}}),prisma.paymentRecord.findMany({where:{tenantId:id}}),prisma.stockMovement.findMany({where:{tenantId:id}}),prisma.auditLog.findMany({where:{tenantId:id}})]);await prisma.tenantExportJob.update({where:{id:job.id},data:{status:'DOWNLOADED'}});res.type('application/json').attachment(`tradeflow-${tenant?.slug??id}-export.json`).send(JSON.stringify({exportedAt:new Date().toISOString(),tenant,users,products,categories,orders,invoices,payments,movements,audits}))});export default router
