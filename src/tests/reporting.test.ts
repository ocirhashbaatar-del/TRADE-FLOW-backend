import{describe,expect,it}from'vitest';import{averageInventory,ledgerValuation}from'../lib/reporting.js'
describe('report formulas',()=>{it('uses daily average instead of current stock',()=>{const end=new Date('2026-01-11T00:00:00Z'),rows=[{productId:'p',quantity:100,createdAt:new Date('2026-01-01T00:00:00Z')},{productId:'p',quantity:-50,createdAt:new Date('2026-01-06T00:00:00Z')}];expect(averageInventory(rows,end,10).get('p')).toBe(75)});it('values outgoing stock at ledger weighted average',()=>{const rows=[{productId:'p',quantity:10,unitCost:100,createdAt:new Date('2026-01-01')},{productId:'p',quantity:10,unitCost:200,createdAt:new Date('2026-01-02')},{productId:'p',quantity:-5,createdAt:new Date('2026-01-03')}];expect(ledgerValuation(rows).get('p')).toEqual({quantity:15,value:2250})})})
describe('10,000 SKU report performance',()=>{it('keeps ledger valuation p95 under 500ms',()=>{const rows=Array.from({length:10000},(_,i)=>({productId:`p-${i}`,quantity:10,unitCost:100+i%20,createdAt:new Date('2026-01-01')}));const times=Array.from({length:20},()=>{const start=performance.now();ledgerValuation(rows);return performance.now()-start}).sort((a,b)=>a-b);expect(times[Math.ceil(times.length*.95)-1]).toBeLessThan(500)})})
